import { PipeFormatPipe } from './pipe-format.pipe';

describe('PipeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
